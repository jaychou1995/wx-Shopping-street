<!--compontents/w-tab-control/w-tab-control.wxml-->
<view class="control">
	<block wx:for="{{titles}}" wx:key="{{index}}">
		<view class='tab-item {{index == currentIndex?"active": ""}}'  bindtap="itemClick" data-index="{{index}}">
			<text>{{item}}</text>
		</view>
	</block>
</view>
